name: Pull Request validation

on: [pull_request]


jobs:
  ASPA2-contracts-ci:
    name: Run ASPA2-contracts validation
    uses: ./.github/workflows/ASPA2-contracts-ci.yaml

  ASPA2-frontend-ci:
    name: Run ASPA2-frontend validation
    uses: ./.github/workflows/ASPA2-frontend-ci.yaml

